node_modules/
.next/
out/
.env
.env.local
.env*.local
coverage/
.pnpm-store/
tsconfig.tsbuildinfo

# Runtime data (JSON store) — never commit
.data/

# Agent session memory — local only
.workbuddy-ai/
